import { Cardo, Inter } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const cardo = Cardo({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
